%tr{:id => "member-#{member.id}"}
  %td= member.name
  %td= member.church
  %td= member.pm_meetings_only ? "Yes" : ""
  %td= member.nursery_needed ? "Yes" : ""
  %td= link_to "x", member, :remote => true, :method => :delete